import type { ChannelPerformance, MarketingChannelData } from '@/shared/types';

export type { ChannelPerformance, MarketingChannelData };

export interface ChannelTableProps {
  selectedMonth: string;
}
